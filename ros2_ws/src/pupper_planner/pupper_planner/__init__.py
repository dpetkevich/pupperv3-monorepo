"""pupper_planner: Plan JSON v1 validation, a rclpy-free step runner, and the plan_executor node.

Sign convention everywhere: ROS/REP-103, ``turn.degrees > 0`` = left, ``move.lateral_m > 0`` = left,
object headings from pupper_vision are already +left.
"""
